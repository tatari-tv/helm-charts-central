apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gha-runner-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "gha-runner-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gha-runner-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
